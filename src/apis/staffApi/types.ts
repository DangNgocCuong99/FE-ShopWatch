import { BaseInfo } from "../types";

export interface IStaff {
    _id: string,
    name: String,
    date_of_birth: Number,
    age: Number,
    country: String,
    id_card: Number,
    phone_number: Number,
}